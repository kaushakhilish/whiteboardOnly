import React from 'react';
// import styles from './shapeProperties.module.css';

const ShapeProperties = ({ rectProps }) => {
    return (
        <rect x={rectProps.x} y={rectProps.y} >
            
        </rect>
    );
}

export default ShapeProperties;
